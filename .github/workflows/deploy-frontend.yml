name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - "nginx/**"
      - ".github/workflows/deploy-frontend.yml"

jobs:
  deploy:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: /home/ubuntu/goodness-energy
    steps:
      - name: Pull latest code
        run: git pull origin main

      - name: Deploy frontend and nginx
        run: docker compose up -d --build frontend nginx
