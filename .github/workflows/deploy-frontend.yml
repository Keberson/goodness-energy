name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - "nginx/**"
      - ".github/workflows/deploy-frontend.yml"

jobs:
  deploy:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: $HOME/goodness-energy
    steps:
      - name: Pull latest code
        run: git pull origin main

      - name: Clean frontend volume
        run: docker compose down frontend nginx && docker volume rm goodness-energy_frontend-build || true

      - name: Deploy frontend and nginx
        run: docker compose up -d --build frontend nginx
