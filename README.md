# Энергия добра

Платформа для взаимодействия некоммерческих организаций (НКО) и волонтеров. Проект разработан для хакатона Social Hack 2025.

## 🌐 Сайт проекта

**Ссылка на сайт:** http://89.169.178.111/

## 📋 Описание решения

"Энергия добра" — это веб-платформа, которая объединяет некоммерческие организации и волонтеров, предоставляя инструменты для:

- **НКО:**
  - Регистрация и управление профилем организации
  - Создание и управление событиями/мероприятиями
  - Публикация новостей и информации о деятельности
  - Просмотр статистики и откликов волонтеров
  - Модерация контента администратором

- **Волонтеры:**
  - Регистрация и управление профилем
  - Просмотр карты с НКО и их расположением
  - Поиск и участие в событиях
  - Сохранение избранных событий и новостей
  - Просмотр базы знаний и образовательных материалов

- **Администраторы:**
  - Модерация НКО и контента
  - Управление базой знаний
  - Административный контроль платформы

## 🏗️ Архитектура проекта

Проект построен на микросервисной архитектуре с использованием Docker Compose:

- **Backend** — REST API на FastAPI (Python)
- **Frontend** — SPA на React + TypeScript + Vite
- **Nginx** — веб-сервер для раздачи статики и проксирования API запросов
- **PostgreSQL** — реляционная база данных
- **MinIO** — объектное хранилище для файлов

**Важно:** CORS отключен в backend, так как все API запросы проксируются через nginx. Фронтенд обращается к API через относительный путь `/api`, который nginx проксирует на backend.

## 📁 Структура проекта

```
goodness-energy/
├── backend/                    # Backend приложение (FastAPI)
│   ├── app/
│   │   ├── routers/           # API роутеры (auth, npo, volunteer, admin, news, files, map, knowledges, events, favorites)
│   │   ├── models.py          # SQLAlchemy модели базы данных
│   │   ├── schemas.py         # Pydantic схемы для валидации
│   │   ├── database.py        # Настройка подключения к БД
│   │   ├── auth.py            # JWT аутентификация
│   │   └── minio_client.py    # Клиент для работы с MinIO
│   ├── main.py                # Точка входа FastAPI приложения
│   ├── requirements.txt       # Python зависимости
│   ├── Dockerfile             # Docker образ для backend
│   ├── init-data.sql          # Начальные данные для БД
│   └── README.md              # Документация backend
│
├── frontend/                   # Frontend приложение (React + TypeScript)
│   ├── src/
│   │   ├── pages/             # Страницы приложения
│   │   │   ├── Auth/          # Авторизация и регистрация
│   │   │   ├── HomePage/      # Главная страница
│   │   │   ├── MapPage/       # Карта с НКО
│   │   │   ├── NPO/           # Страницы для НКО
│   │   │   ├── Volunteer/     # Страницы для волонтеров
│   │   │   ├── News/          # Новости
│   │   │   ├── Knowledges/    # База знаний
│   │   │   ├── Events/        # События
│   │   │   └── Admin/         # Административные страницы
│   │   ├── services/          # API сервисы и Redux store
│   │   ├── common/            # Общие компоненты
│   │   ├── hooks/             # React хуки
│   │   ├── types/             # TypeScript типы
│   │   └── router.tsx         # Роутинг приложения
│   ├── public/                # Статические файлы
│   ├── package.json           # Node.js зависимости
│   ├── vite.config.ts         # Конфигурация Vite
│   └── Dockerfile             # Docker образ для frontend
│
├── nginx/                      # Nginx конфигурация
│   ├── nginx.conf             # Конфигурация веб-сервера
│   └── Dockerfile             # Docker образ для Nginx
│
├── .github/
│   └── workflows/              # GitHub Actions для CI/CD
│       ├── deploy-backend.yml  # Автоматический деплой backend
│       └── deploy-frontend.yml # Автоматический деплой frontend
│
├── docker-compose.yaml         # Docker Compose конфигурация
└── README.md                   # Этот файл
```

## 🚀 Запуск проекта

### Предварительные требования

- Docker и Docker Compose
- Git

### Быстрый старт с Docker Compose

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd goodness-energy
   ```

2. **Создайте файл `.env` в корне проекта:**
   ```env
   # База данных
   POSTGRES_USER=postgres
   POSTGRES_PASSWORD=postgres
   POSTGRES_DB=social_hack
   POSTGRES_PORT=5432

   # Backend
   BACKEND_PORT=8000
   SECRET_KEY=your-secret-key-here

   # MinIO
   MINIO_ROOT_USER=minioadmin
   MINIO_ROOT_PASSWORD=minioadmin
   MINIO_PORT=9000
   MINIO_CONSOLE_PORT=9001
   MINIO_BUCKET=files

   # Nginx
   NGINX_PORT=80
   ```

3. **Запустите все сервисы:**
   ```bash
   docker-compose up -d
   ```

4. **Проверьте статус контейнеров:**
   ```bash
   docker-compose ps
   ```

5. **Приложение будет доступно:**
   - Frontend: http://localhost (или порт указанный в NGINX_PORT)
   - Backend API: http://localhost:8000
   - API документация (Swagger): http://localhost:8000/docs
   - MinIO консоль: http://localhost:9001

### Остановка проекта

```bash
docker-compose down
```

Для полной очистки данных (volumes):
```bash
docker-compose down -v
```

### Локальная разработка

#### Backend

1. Перейдите в директорию backend:
   ```bash
   cd backend
   ```

2. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Windows: venv\Scripts\activate
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Запустите сервер:
   ```bash
   uvicorn main:app --reload
   ```

Backend будет доступен по адресу: http://localhost:8000

#### Frontend

1. Перейдите в директорию frontend:
   ```bash
   cd frontend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env` с переменными окружения:
   ```env
   # Для локальной разработки используйте прямой URL к backend
   VITE_API_BASE_URL=http://localhost:8000
   
   # Для продакшена (через nginx прокси) используйте относительный путь
   # VITE_API_BASE_URL=/api
   ```

4. Запустите dev-сервер:
   ```bash
   npm run dev
   ```

Frontend будет доступен по адресу: http://localhost:5173

**Примечание:** 
- Для локальной разработки необходимо также запустить PostgreSQL и MinIO через Docker Compose или настроить их локально.
- В продакшене API запросы проксируются через nginx с префиксом `/api`, поэтому переменная `VITE_API_BASE_URL` должна быть установлена в `/api` при сборке фронтенда для продакшена.

## 🔄 CI/CD через GitHub Actions

Проект использует GitHub Actions для автоматического развертывания при изменении кода в ветке `main`.

### Workflows

1. **Deploy Backend** (`.github/workflows/deploy-backend.yml`)
   - Триггер: изменения в `backend/**` или самом workflow файле
   - Действия:
     - Pull последнего кода из репозитория
     - Пересборка и перезапуск backend контейнера

2. **Deploy Frontend** (`.github/workflows/deploy-frontend.yml`)
   - Триггер: изменения в `frontend/**`, `nginx/**` или самом workflow файле
   - Действия:
     - Pull последнего кода из репозитория
     - Очистка volumes для frontend и nginx
     - Пересборка и перезапуск frontend и nginx контейнеров

### Требования для CI/CD

- Self-hosted runner на сервере развертывания
- Docker и Docker Compose установлены на сервере
- Проект должен быть клонирован в `/home/ubuntu/goodness-energy`

## 🛠️ Технологии

### Backend
- **FastAPI** — современный веб-фреймворк для Python
- **SQLAlchemy** — ORM для работы с базой данных
- **Pydantic** — валидация данных
- **JWT** — аутентификация пользователей
- **MinIO** — объектное хранилище для файлов
- **PostgreSQL** — реляционная база данных

### Frontend
- **React 19** — библиотека для создания UI
- **TypeScript** — типизированный JavaScript
- **Vite** — быстрый сборщик и dev-сервер
- **Redux Toolkit** — управление состоянием
- **React Router** — маршрутизация
- **Ant Design** — UI компоненты
- **Yandex Maps** — карты для отображения НКО
- **SCSS** — препроцессор CSS

### Инфраструктура
- **Docker** — контейнеризация
- **Docker Compose** — оркестрация контейнеров
- **Nginx** — веб-сервер
- **GitHub Actions** — CI/CD

## 📚 Дополнительная документация

- [Backend API документация](backend/README.md)
- [Backend API endpoints](backend/API_DOCS.md)

## 👥 Команда

Проект разработан для хакатона Social Hack 2025.

## 📝 Лицензия

[Укажите лицензию проекта]