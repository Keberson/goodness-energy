# Энергия добра

Платформа для взаимодействия некоммерческих организаций (НКО) и волонтеров. Проект разработан для хакатона Social Hack 2025.

## 🌐 Сайт проекта

**Ссылка на сайт:** http://89.169.178.111/

## 🎥 Видео проекта

Демонстрационные видео проекта:

-   [Видео 1](../videos/video1.mp4)
-   [Видео 2](../videos/video2.mp4)
-   [Видео 3](../videos/video3.mp4)
-   [Видео 4](../videos/video4.mp4)
-   [Видео 5](../videos/video5.mp4)
-   [Видео 6](../videos/video6.mp4)

## 📋 Описание решения

"Энергия добра" — это веб-платформа, которая объединяет некоммерческие организации и волонтеров, предоставляя инструменты для:

-   **НКО:**

    -   Регистрация и управление профилем организации
    -   Создание и управление событиями/мероприятиями
    -   Публикация новостей и информации о деятельности
    -   Просмотр статистики и откликов волонтеров
    -   Загрузка фотографий в галерею организации
    -   Управление тегами и ссылками на социальные сети

-   **Волонтеры:**

    -   Регистрация и управление профилем
    -   Просмотр карты с НКО и их расположением
    -   Поиск и участие в событиях
    -   Сохранение избранных событий, новостей и материалов базы знаний
    -   Просмотр базы знаний и образовательных материалов
    -   Публикация собственных блогов

-   **Администраторы:**
    -   Модерация НКО (подтверждение/отклонение регистраций)
    -   Управление базой знаний (создание, редактирование, удаление материалов)
    -   Удаление событий и контента
    -   Административный контроль платформы

## 🏗️ Архитектура проекта

Проект построен на микросервисной архитектуре с использованием Docker Compose:

-   **Backend** — REST API на FastAPI (Python)
-   **Frontend** — SPA на React + TypeScript + Vite
-   **Nginx** — веб-сервер для раздачи статики и проксирования API запросов
-   **PostgreSQL** — реляционная база данных
-   **MinIO** — объектное хранилище для файлов

## 📁 Структура проекта

```
goodness-energy/
├── backend/                    # Backend приложение (FastAPI)
│   ├── app/
│   │   ├── routers/           # API роутеры (auth, npo, volunteer, admin, news, files, map, knowledges, events, favorites)
│   │   ├── models.py          # SQLAlchemy модели базы данных
│   │   ├── schemas.py         # Pydantic схемы для валидации
│   │   ├── database.py        # Настройка подключения к БД
│   │   ├── auth.py            # JWT аутентификация
│   │   └── minio_client.py    # Клиент для работы с MinIO
│   ├── main.py                # Точка входа FastAPI приложения
│   ├── requirements.txt       # Python зависимости
│   ├── Dockerfile             # Docker образ для backend
│   └── init-data.sql          # Начальные данные для БД
│
├── frontend/                   # Frontend приложение (React + TypeScript)
│   ├── src/
│   │   ├── pages/             # Страницы приложения
│   │   │   ├── Auth/          # Авторизация и регистрация
│   │   │   ├── HomePage/      # Главная страница
│   │   │   ├── MapPage/       # Карта с НКО
│   │   │   ├── NPO/           # Страницы для НКО
│   │   │   ├── Volunteer/     # Страницы для волонтеров
│   │   │   ├── News/          # Новости
│   │   │   ├── Knowledges/    # База знаний
│   │   │   ├── Events/        # События
│   │   │   └── Admin/         # Административные страницы
│   │   ├── services/          # API сервисы и Redux store
│   │   ├── common/            # Общие компоненты
│   │   ├── hooks/             # React хуки
│   │   ├── types/             # TypeScript типы
│   │   └── router.tsx         # Роутинг приложения
│   ├── public/                # Статические файлы
│   ├── package.json           # Node.js зависимости
│   ├── vite.config.ts         # Конфигурация Vite
│   └── Dockerfile             # Docker образ для frontend
│
├── nginx/                      # Nginx конфигурация
│   ├── nginx.conf             # Конфигурация веб-сервера
│   └── Dockerfile             # Docker образ для Nginx
│
├── .github/
│   └── workflows/              # GitHub Actions для CI/CD
│       ├── deploy-backend.yml  # Автоматический деплой backend
│       └── deploy-frontend.yml # Автоматический деплой frontend
│
├── docker-compose.yaml         # Docker Compose конфигурация
└── README.md                   # Этот файл
```

## 🚀 Запуск проекта

### Предварительные требования

-   Docker и Docker Compose
-   Git

### Быстрый старт с Docker Compose

1. **Клонируйте репозиторий:**

    ```bash
    git clone git@github.com:Keberson/goodness-energy.git
    cd goodness-energy
    ```

2. **Создайте файл `.env` в корне проекта:**

    ```env
    # База данных
    POSTGRES_USER=postgres
    POSTGRES_PASSWORD=postgres
    POSTGRES_DB=social_hack
    POSTGRES_PORT=5432

    # Backend
    BACKEND_PORT=8000
    SECRET_KEY=your-secret-key-here

    # MinIO
    MINIO_ROOT_USER=minioadmin
    MINIO_ROOT_PASSWORD=minioadmin
    MINIO_PORT=9000
    MINIO_CONSOLE_PORT=9001
    MINIO_BUCKET=files

    # Nginx
    NGINX_PORT=80

    # SMTP (для отправки email уведомлений)
    SMTP_HOST=smtp.mail.ru
    SMTP_PORT=465
    SMTP_USER=your-email@mail.ru
    SMTP_PASSWORD=your-app-password
    SMTP_FROM_EMAIL=your-email@mail.ru
    SMTP_USE_TLS=false

    # Frontend URL (для ссылок в email уведомлениях)
    FRONTEND_BASE_URL=http://localhost
    # FRONTEND_URL - опционально, используется для VK OAuth (если не указан, берется из FRONTEND_BASE_URL)
    # FRONTEND_URL=http://localhost:5173

    # VK OAuth (для авторизации через VK)
    VK_CLIENT_ID=your-vk-app-id
    VK_CLIENT_SECRET=your-vk-secret-key
    # VK_REDIRECT_URI - опционально (автоматически формируется из FRONTEND_URL или FRONTEND_BASE_URL)
    # VK_REDIRECT_URI=http://localhost:5173/auth/vk/callback

    # OpenRouter API (для автоматической модерации новостей через LLM)
    # Получить API ключ можно на https://openrouter.ai/
    OPENROUTER_API_KEY=your-openrouter-api-key
    # Модель для модерации (по умолчанию: google/gemini-flash-1.5)
    # OPENROUTER_MODEL=google/gemini-flash-1.5
    # URL вашего сайта для OpenRouter (по умолчанию: https://goodness-energy.ru)
    # OPENROUTER_REFERER=https://goodness-energy.ru
    ```

    **Примечание по настройке VK OAuth:**

    - Создайте приложение на https://vk.com/apps?act=manage
    - Скопируйте `Application ID` (это `VK_CLIENT_ID`) и `Secure key` (это `VK_CLIENT_SECRET`)
    - `VK_REDIRECT_URI` формируется автоматически из `FRONTEND_URL` или `FRONTEND_BASE_URL` + `/auth/vk/callback`
    - Если нужно указать другой redirect URI, задайте `VK_REDIRECT_URI` явно
    - В настройках VK приложения укажите Redirect URI: `http://localhost:5173/auth/vk/callback` (для разработки) или ваш production URL

    **Примечание по настройке SMTP:**

    - Для Mail.ru: используйте порт 465 с SSL (SMTP_USE_TLS=false). Создайте пароль приложения: https://id.mail.ru/profile → Безопасность → Пароли приложений
    - Для Gmail: используйте "Пароль приложения" вместо обычного пароля (настройки аккаунта → Безопасность → Двухэтапная аутентификация → Пароли приложений), порт 587, SMTP_USE_TLS=true
    - Для Yandex: используйте порт 465 и SMTP_USE_TLS=false (или порт 587 с SMTP_USE_TLS=true)
    - Если SMTP настройки не указаны, уведомления на email отправляться не будут (в логах будет предупреждение)

    ```

    ```

3. **Запустите все сервисы:**

    ```bash
    docker-compose up -d
    ```

4. **Проверьте статус контейнеров:**

    ```bash
    docker-compose ps
    ```

5. **Приложение будет доступно:**
    - Frontend: http://localhost (или порт указанный в NGINX_PORT)
    - Backend API: http://localhost:8000/api
    - API документация (Swagger): http://localhost:8000/docs
    - MinIO консоль: http://localhost:9001

### Остановка проекта

```bash
docker-compose down
```

Для полной очистки данных (volumes):

```bash
docker-compose down -v
```

### Локальная разработка

#### Backend

1. Перейдите в директорию backend:

    ```bash
    cd backend
    ```

2. Создайте виртуальное окружение:

    ```bash
    python -m venv venv
    source venv/bin/activate  # Windows: venv\Scripts\activate
    ```

3. Установите зависимости:

    ```bash
    pip install -r requirements.txt
    ```

4. Запустите сервер:
    ```bash
    uvicorn main:app --reload
    ```

Backend будет доступен по адресу: http://localhost:8000

#### Frontend

1. Перейдите в директорию frontend:

    ```bash
    cd frontend
    ```

2. Установите зависимости:

    ```bash
    npm install
    ```

3. Создайте файл `.env` с переменными окружения:

    ```env
    # Для локальной разработки используйте прямой URL к backend
    VITE_API_BASE_URL=http://localhost:8000

    # Для продакшена (через nginx прокси) используйте относительный путь
    # VITE_API_BASE_URL=/api
    ```

4. Запустите dev-сервер:
    ```bash
    npm run dev
    ```

Frontend будет доступен по адресу: http://localhost:5173

**Примечание:**

-   Для локальной разработки необходимо также запустить PostgreSQL и MinIO через Docker Compose или настроить их локально.
-   В продакшене API запросы проксируются через nginx с префиксом `/api`, поэтому переменная `VITE_API_BASE_URL` должна быть установлена в `/api` при сборке фронтенда для продакшена.

## 🔄 CI/CD через GitHub Actions

Проект использует GitHub Actions для автоматического развертывания при изменении кода в ветке `main`.

### Workflows

1. **Deploy Backend** (`.github/workflows/deploy-backend.yml`)

    - Триггер: изменения в `backend/**` или самом workflow файле
    - Действия:
        - Pull последнего кода из репозитория
        - Пересборка и перезапуск backend контейнера

2. **Deploy Frontend** (`.github/workflows/deploy-frontend.yml`)
    - Триггер: изменения в `frontend/**`, `nginx/**` или самом workflow файле
    - Действия:
        - Pull последнего кода из репозитория
        - Очистка volumes для frontend и nginx
        - Пересборка и перезапуск frontend и nginx контейнеров

### Требования для CI/CD

-   Self-hosted runner на сервере развертывания
-   Docker и Docker Compose установлены на сервере
-   Проект должен быть клонирован в `/home/ubuntu/goodness-energy`

## 🔌 Структура API

API построен на REST архитектуре и использует JWT токены для аутентификации. Все защищенные эндпоинты требуют Bearer токен в заголовке `Authorization: Bearer {token}`.

### Базовый URL

-   **Production**: `http://89.169.178.111/api`
-   **Development**: `http://localhost:8000/api`
-   **Swagger документация**: `http://localhost:8000/docs`

### Роли пользователей

-   **`npo`** — Некоммерческая организация
-   **`volunteer`** — Волонтер
-   **`admin`** — Администратор

### Статусы

**Статусы НКО:**

-   `confirmed` — подтверждена администратором
-   `not_confirmed` — ожидает модерации

**Статусы событий:**

-   `draft` — черновик
-   `published` — опубликовано
-   `cancelled` — отменено
-   `completed` — завершено

**Типы новостей:**

-   `blog` — блог-пост
-   `edu` — образовательный материал
-   `docs` — документация

### Эндпоинты API

#### 🔐 Аутентификация (`/auth`)

| Метод | Эндпоинт        | Описание              | Требует авторизации |
| ----- | --------------- | --------------------- | ------------------- |
| POST  | `/auth/reg/npo` | Регистрация НКО       | ❌                  |
| POST  | `/auth/reg/vol` | Регистрация волонтера | ❌                  |
| POST  | `/auth/login`   | Вход в систему        | ❌                  |

#### 🗺️ Карта (`/map`)

| Метод | Эндпоинт   | Описание                      | Требует авторизации |
| ----- | ---------- | ----------------------------- | ------------------- |
| GET   | `/map/npo` | Получение точек НКО для карты | ❌                  |

Возвращает массив объектов с координатами и полной информацией о НКО.

#### 🏢 НКО (`/npo`)

| Метод  | Эндпоинт                                | Описание                  | Требует авторизации | Роль |
| ------ | --------------------------------------- | ------------------------- | ------------------- | ---- |
| GET    | `/npo`                                  | Список всех НКО           | ❌                  | -    |
| GET    | `/npo/tags`                             | Список всех тегов НКО     | ❌                  | -    |
| GET    | `/npo/{npo_id}`                         | Информация об НКО по ID   | ❌                  | -    |
| PUT    | `/npo/{npo_id}`                         | Обновление профиля НКО    | ✅                  | NPO  |
| DELETE | `/npo/{npo_id}`                         | Удаление НКО              | ✅                  | NPO  |
| GET    | `/npo/{npo_id}/statistics`              | Статистика НКО            | ✅                  | NPO  |
| POST   | `/npo/{npo_id}/event`                   | Создание события          | ✅                  | NPO  |
| PUT    | `/npo/{npo_id}/event/{event_id}`        | Обновление события        | ✅                  | NPO  |
| DELETE | `/npo/{npo_id}/event/{event_id}`        | Удаление события          | ✅                  | NPO  |
| PATCH  | `/npo/{npo_id}/event/{event_id}/status` | Изменение статуса события | ✅                  | NPO  |
| POST   | `/npo/{npo_id}/news`                    | Публикация новости        | ✅                  | NPO  |
| GET    | `/npo/{npo_id}/events`                  | Список событий НКО        | ❌                  | -    |
| GET    | `/npo/{npo_id}/news`                    | Список новостей НКО       | ❌                  | -    |

#### 👤 Волонтеры (`/volunteer`)

| Метод  | Эндпоинт                                | Описание                | Требует авторизации | Роль      |
| ------ | --------------------------------------- | ----------------------- | ------------------- | --------- |
| GET    | `/volunteer`                            | Список всех волонтеров  | ❌                  | -         |
| GET    | `/volunteer/{volunteer_id}`             | Информация о волонтере  | ❌                  | -         |
| PUT    | `/volunteer/{volunteer_id}`             | Обновление профиля      | ✅                  | Volunteer |
| DELETE | `/volunteer/{volunteer_id}`             | Удаление профиля        | ✅                  | Volunteer |
| POST   | `/volunteer/event/{event_id}`           | Отклик на событие       | ✅                  | Volunteer |
| DELETE | `/volunteer/event/{event_id}/{user_id}` | Удаление отклика        | ✅                  | Volunteer |
| GET    | `/volunteer/{volunteer_id}/events`      | Мои события (отклики)   | ✅                  | Volunteer |
| POST   | `/volunteer/{volunteer_id}/news`        | Создание новости (blog) | ✅                  | Volunteer |

#### 📰 Новости (`/news`)

| Метод  | Эндпоинт          | Описание               | Требует авторизации | Роль        |
| ------ | ----------------- | ---------------------- | ------------------- | ----------- |
| GET    | `/news`           | Список всех новостей   | ❌                  | -           |
| GET    | `/news/{news_id}` | Новость по ID          | ❌                  | -           |
| POST   | `/news`           | Создание новости       | ✅                  | Все         |
| PUT    | `/news/{news_id}` | Редактирование новости | ✅                  | Автор/Admin |
| DELETE | `/news/{news_id}` | Удаление новости       | ✅                  | Автор/Admin |

#### 📚 База знаний (`/knowledges`)

| Метод  | Эндпоинт                              | Описание                              | Требует авторизации | Роль  |
| ------ | ------------------------------------- | ------------------------------------- | ------------------- | ----- |
| GET    | `/knowledges`                         | Список всех материалов                | ❌                  | -     |
| GET    | `/knowledges/{knowledge_id}`          | Материал по ID                        | ❌                  | -     |
| POST   | `/knowledges`                         | Создание материала                    | ✅                  | Admin |
| PUT    | `/knowledges/{knowledge_id}`          | Редактирование материала              | ✅                  | Admin |
| DELETE | `/knowledges/{knowledge_id}`          | Удаление материала                    | ✅                  | Admin |
| GET    | `/knowledges/{knowledge_id}/download` | Скачивание файлов (ZIP или один файл) | ❌                  | -     |

#### 🎯 События (`/events`)

| Метод | Эндпоинт  | Описание            | Требует авторизации |
| ----- | --------- | ------------------- | ------------------- |
| GET   | `/events` | Список всех событий | ❌                  |

#### ⭐ Избранное (`/favorites`)

| Метод  | Эндпоинт                                 | Описание                     | Требует авторизации | Роль |
| ------ | ---------------------------------------- | ---------------------------- | ------------------- | ---- |
| GET    | `/favorites`                             | Список избранного            | ✅                  | Все  |
| POST   | `/favorites`                             | Добавление в избранное       | ✅                  | Все  |
| DELETE | `/favorites/{item_type}/{item_id}`       | Удаление из избранного       | ✅                  | Все  |
| GET    | `/favorites/check/{item_type}/{item_id}` | Проверка наличия в избранном | ✅                  | Все  |

**Типы элементов избранного:**

-   `news` — новость
-   `event` — событие
-   `knowledge` — материал базы знаний

#### 📁 Файлы (`/files`)

| Метод  | Эндпоинт                | Описание           | Требует авторизации | Роль |
| ------ | ----------------------- | ------------------ | ------------------- | ---- |
| POST   | `/files`                | Загрузка файла     | ✅                  | Все  |
| GET    | `/files/{file_id}`      | Получение файла    | ❌                  | -    |
| GET    | `/files/{file_id}/info` | Информация о файле | ❌                  | -    |
| DELETE | `/files/{file_id}`      | Удаление файла     | ✅                  | Все  |

**Поддерживаемые форматы файлов:** `jpg`, `jpeg`, `png`, `pdf`, `docx`, `doc`, `xlsx`, `txt`, `csv`

#### 👨‍💼 Администратор (`/admin`)

| Метод  | Эндпоинт                     | Описание                    | Требует авторизации | Роль  |
| ------ | ---------------------------- | --------------------------- | ------------------- | ----- |
| GET    | `/admin/npo/unconfirmed`     | Список неподтвержденных НКО | ✅                  | Admin |
| PATCH  | `/admin/npo/{npo_id}/status` | Изменение статуса НКО       | ✅                  | Admin |
| DELETE | `/admin/npo/{npo_id}`        | Удаление НКО                | ✅                  | Admin |
| DELETE | `/admin/event/{event_id}`    | Удаление события            | ✅                  | Admin |

### Коды ответов

-   `200 OK` — успешный запрос
-   `201 Created` — ресурс успешно создан
-   `204 No Content` — успешное удаление
-   `400 Bad Request` — неверный запрос
-   `401 Unauthorized` — требуется авторизация
-   `403 Forbidden` — недостаточно прав
-   `404 Not Found` — ресурс не найден
-   `500 Internal Server Error` — внутренняя ошибка сервера

## 🛠️ Технологии

### Backend

-   **FastAPI** — современный веб-фреймворк для Python
-   **SQLAlchemy** — ORM для работы с базой данных
-   **Pydantic** — валидация данных
-   **JWT** — аутентификация пользователей
-   **MinIO** — объектное хранилище для файлов
-   **PostgreSQL** — реляционная база данных

### Frontend

-   **React 19** — библиотека для создания UI
-   **TypeScript** — типизированный JavaScript
-   **Vite** — быстрый сборщик и dev-сервер
-   **Redux Toolkit** — управление состоянием
-   **React Router** — маршрутизация
-   **Ant Design** — UI компоненты
-   **Yandex Maps** — карты для отображения НКО
-   **SCSS** — препроцессор CSS

### Инфраструктура

-   **Docker** — контейнеризация
-   **Docker Compose** — оркестрация контейнеров
-   **Nginx** — веб-сервер
-   **GitHub Actions** — CI/CD

## 📖 Дополнительная документация

Подробная документация API доступна через Swagger UI:

-   **Локально**: http://localhost:8000/docs
-   **Production**: http://89.169.178.111:8000/docs

## 👥 Команда

Проект разработан для хакатона Social Hack 2025 командой Крутые бобры 😎 (Кузов Максим, Миронов Андрей - ВИШ МИФИ)
